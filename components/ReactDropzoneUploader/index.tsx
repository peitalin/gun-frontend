import {
  formatBytes,
  formatDuration,
  accepts,
  defaultClassNames,
  mergeStyles,
  getFilesFromEvent,
} from "./utils";
import {
  StatusValue,
  IMeta,
  IFileWithMeta,
  IExtra,
  IUploadParams,
  CustomizationFunction,
  IStyleCustomization,
  IExtraLayout,
  ILayoutProps,
  IPreviewProps ,
  IInputProps,
  ISubmitButtonProps,
  IDropzoneProps,
} from "./Dropzone";



export default {
  // utils.ts
  formatBytes,
  formatDuration,
  accepts,
  defaultClassNames,
  mergeStyles,
  getFilesFromEvent,
};